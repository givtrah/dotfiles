{
    "sway/workspaces": {
    "format": "{id}",
    "persistent-workspaces": {
      "1": [], // Always show a workspace with name 'X', on all outputs if it does not exists
      "2": [], // Always show a workspace with name 'X', on all outputs if it does not exists
      "3": [], // Always show a workspace with name 'X', on all outputs if it does not exists
      "4": [], // Always show a workspace with name 'X', on all outputs if it does not exists
      "5": [], // Always show a workspace with name 'X', on all outputs if it does not exists
      "6": [], // Always show a workspace with name 'X', on all outputs if it does not exists
      "7": [], // Always show a workspace with name 'X', on all outputs if it does not exists
        },
    },
}
